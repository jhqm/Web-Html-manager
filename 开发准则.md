# 开发准则

> 本文档用于沉淀开发过程中的经验，并作为每次开发动作前的强制校验基线。

## 核心准则

1. 每次在 IDE 中进行单独的 `write in file` 修改，可能不会真正落盘保存；多次分散写入时，往往只有最后一次缓冲区结果可见。若人工仅执行一次 `Cmd+S`，通常只会保存最后一次修改。
2. **磁盘回读校验必须在 `Cmd+S` 之后执行**；`Cmd+S` 之前只能做缓冲区（IDE 可见内容）校验。
3. 同一文件涉及多处改动时，优先“合并为一次写入”，避免多次分散写入导致前序改动被覆盖。
4. 已有文件优先使用差量修改（如局部替换），减少整文件覆盖带来的漂移风险。

## 标准校验流程（强制）

1. **缓冲区校验**：AI 修改后，先检查 IDE 当前内容是否符合预期。
2. **人工保存**：用户执行 `Cmd+S`。
3. **磁盘回读校验**：保存后读取磁盘文件，核对关键标记与改动是否完整存在。
4. **运行验证**：刷新/运行后验证功能与视觉结果。

## 执行要求

- 每次开发前，先阅读本文件并做准则校验，确认本次改动不违反已有规则。
- 每次问题复盘后，将可复用经验追加到本文件，保持持续更新。
- 若准则与现实行为冲突，以“可验证、可落盘、可回读”为最高优先级修订准则。

## 校验记录

- 2026-05-14：纳入首条准则（多次分散写入可能仅保留最后一次缓冲区结果）。
- 2026-05-14：补充 `Cmd+S` 前后校验边界，确立“缓冲区校验 → 保存 → 磁盘回读”的分层流程。
