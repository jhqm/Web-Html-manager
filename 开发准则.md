# 开发准则

> 本文档用于沉淀开发过程中的经验，并作为每次开发动作前的强制校验基线。

## 核心准则

1. 每次在 IDE 中进行单独的 `write in file` 修改，可能不会真正落盘保存；多次分散写入时，往往只有最后一次缓冲区结果可见。若人工仅执行一次 `Cmd+S`，通常只会保存最后一次修改。
2. **磁盘回读校验必须在 `Cmd+S` 之后执行**；`Cmd+S` 之前只能做缓冲区（IDE 可见内容）校验。
3. 同一文件涉及多处改动时，优先“合并为一次写入”，避免多次分散写入导致前序改动被覆盖。
4. 已有文件优先使用差量修改（如局部替换），减少整文件覆盖带来的漂移风险。
5. 同一文件若涉及多处修改，必须采用"单次原子写盘"（一次脚本/一次写入）完成，禁止在同一轮内对该文件进行多次分散写入。
6. 原子写盘后必须立即执行关键点回读核验（函数名、命令字、版本号等），以磁盘结果作为唯一验收依据。
7. 每次代码变更（含功能、样式、交互、修复）后，必须同步更新版本指纹（如 `buildTag`）；未更新版本指纹视为本次变更未完成。

## 标准校验流程（强制）

1. **开发前校验准则**：开始改动前先阅读本文件，明确本轮是否涉及单文件多处修改。
2. **汇总改单清单**：同一文件的全部改动先汇总，不在同一轮里分散多次写入。
3. **单次原子写盘**：对同一文件一次性完成写盘（一次脚本/一次写入），避免前序改动被覆盖。
4. **磁盘回读验收**：写盘后立即回读磁盘文件，核对关键点（函数名、命令字、版本号）是否完整落盘。
5. **IDE 同步与人工保存**：若文件已在 IDE 打开，先执行“从磁盘重新加载”，再 `Cmd+S`，避免缓冲区冲突覆盖。
6. **运行验证**：刷新/运行后验证功能与视觉结果。
7. **版本指纹刷新**：每次代码变更后立即更新版本指纹，并在回读验收中确认该指纹已落盘。

## 执行要求

- 每次开发前，先阅读本文件并做准则校验，确认本次改动不违反已有规则。
- 每次问题复盘后，将可复用经验追加到本文件，保持持续更新。
- 若准则与现实行为冲突，以“可验证、可落盘、可回读”为最高优先级修订准则。

## 校验记录

- 2026-05-14：纳入首条准则（多次分散写入可能仅保留最后一次缓冲区结果）。
- 2026-05-14：补充 `Cmd+S` 前后校验边界，确立“缓冲区校验 → 保存 → 磁盘回读”的分层流程。
- 2026-05-14：新增“同一文件单次原子写盘”规则，避免多次分散写入仅保留最后一次的问题。
- 2026-05-14：更新标准校验流程为“汇总改单 → 单次原子写盘 → 磁盘回读验收 → IDE 重载后保存”。
- 2026-05-14：新增“每次代码变更必须刷新版本指纹”的强制规则，并纳入标准校验流程。
