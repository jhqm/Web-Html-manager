# AI HTML Asset Manager - 产品规格文档

## 1. 产品概述

### 1.1 产品定位
**个人AI生成HTML资产管理器** - 统一管理所有AI工具（GPT、Claude、豆包、Agent等）产出的HTML文件，将零散的AI产出变成可沉淀、可检索、可迭代的知识库。

### 1.2 核心价值
- 填补传统文档工具（Notion/飞书/腾讯文档）无法原生托管本地HTML的空白
- 把「零散AI HTML产出」变成个人可复用、可追溯的方案库
- 适合：产品经理、运营、PM、游戏策划、咨询、自媒体等高频使用AI做方案的人群

### 1.3 产品形态
- **V1.0 (MVP)**: 桌面客户端（Vue3 + Electron）✅ 已完成
- **V2.0**: 浏览器插件（暂缓）
- **V3.0**: 在线Web云端同步（暂缓）

> 注：当前版本聚焦桌面端应用，暂不考虑浏览器插件和云端功能。

---

## 2. 功能规划

### 2.1 MVP核心功能

#### 2.1.1 仓库管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 自定义仓库目录 | 用户指定一个本地文件夹作为HTML资产仓库 | P0 |
| 手动添加HTML | 通过按钮添加本地HTML文件 | P0 |
| 自动扫描导入 | 启动时自动扫描仓库目录下的所有HTML文件 | P1 |

#### 2.1.2 文件管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 树形文件夹结构 | 左侧边栏显示文件夹目录树 | P0 |
| 文件列表视图 | 中间区域显示HTML文件列表（标题、时间、大小） | P0 |
| 搜索功能 | 支持按文件名、内容搜索 | P0 |
| 标签系统 | 为文件添加/管理自定义标签 | P1 |
| 排序功能 | 按时间/名称/大小排序 | P0 |

#### 2.1.3 预览功能
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 内置WebView预览 | 右侧面板实时渲染HTML，无需打开外部浏览器 | P0 |
| 全屏预览 | 双击文件或按钮全屏预览 | P1 |
| 外部打开 | 一键使用系统默认浏览器打开 | P1 |

#### 2.1.4 版本历史
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 版本快照 | 每次检测到文件变化时自动保存快照 | P0 |
| 版本列表 | 查看历史版本及变更时间 | P0 |
| 版本回滚 | 一键恢复到历史版本 | P0 |
| 版本对比 | 可选：两个版本之间的差异对比 | P2 |

#### 2.1.5 元数据管理
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 自动提取标题 | 从HTML `<title>` 标签提取 | P1 |
| 自动提取描述 | 从 `<meta description>` 提取 | P1 |
| 自动记录来源 | 记录文件创建时间、最后修改时间 | P0 |
| 自动记录文件大小 | 显示HTML文件大小 | P0 |
| 手动备注 | 用户可添加自定义备注 | P1 |

### 2.2 V2.0进阶功能

#### 2.2.1 浏览器插件
- 拦截AI网站（豆包、Claude、GPT等）产出的HTML代码
- 一键保存到本地客户端
- 自动识别并导入

#### 2.2.2 智能标签
- 按行业分类自动打标（产品/运营/策划/调研等）
- 按方案类型打标（PPT/方案/Demo/报告等）
- AI辅助自动分类

#### 2.2.3 导出功能
- HTML转PDF
- HTML转图片
- 生成分享链接

### 2.3 V3.0云端功能
- 云端同步
- 多设备访问
- 分享协作
- 付费会员体系

---

## 3. 技术架构

### 3.1 技术栈
| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 桌面框架 | Electron | 跨平台Win/Mac，内嵌WebView |
| 前端框架 | Vue 3 | 组合式API + Composition |
| 构建工具 | Vite | 快速热更新 |
| 状态管理 | Pinia | Vue官方推荐 |
| UI组件库 | Element Plus | 快速搭建中后台界面 |
| 本地数据库 | SQLite | 轻量级存储文件索引和元数据 |
| 版本控制 | 文件快照 | 版本目录存储历史副本 |

### 3.2 项目结构
```
web-html-manager/
├── electron/
│   ├── main.ts           # Electron主进程
│   ├── preload.ts        # 预加载脚本
│   └── ipc/              # IPC通信模块
├── src/
│   ├── assets/           # 静态资源
│   ├── components/       # Vue组件
│   │   ├── Sidebar/      # 侧边栏组件
│   │   ├── FileList/     # 文件列表组件
│   │   ├── Preview/      # 预览面板组件
│   │   └── common/       # 通用组件
│   ├── composables/      # 组合式函数
│   ├── stores/           # Pinia状态管理
│   ├── types/            # TypeScript类型定义
│   ├── views/            # 页面视图
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
├── electron-builder.json
└── tsconfig.json
```

### 3.3 数据模型

#### 3.3.1 文件表 (files)
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 文件名 |
| path | TEXT | 相对仓库的路径 |
| title | TEXT | HTML标题（自动提取） |
| description | TEXT | HTML描述 |
| size | INTEGER | 文件大小（字节） |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 最后修改时间 |
| folder_id | INTEGER | 所属文件夹 |

#### 3.3.2 文件夹表 (folders)
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 文件夹名 |
| parent_id | INTEGER | 父文件夹ID |
| path | TEXT | 绝对路径 |
| created_at | TEXT | 创建时间 |

#### 3.3.3 版本表 (versions)
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER | 主键 |
| file_id | INTEGER | 关联文件ID |
| version_path | TEXT | 版本文件路径 |
| snapshot_at | TEXT | 快照时间 |
| remark | TEXT | 版本备注 |

#### 3.3.4 标签表 (tags)
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 标签名 |
| color | TEXT | 标签颜色 |

#### 3.3.5 文件标签关联表 (file_tags)
| 字段 | 类型 | 描述 |
|------|------|------|
| file_id | INTEGER | 文件ID |
| tag_id | INTEGER | 标签ID |

---

## 4. 界面设计

### 4.1 整体布局
```
┌────────────────────────────────────────────────────────────────┐
│  Header: Logo + 搜索框 + 设置按钮                              │
├────────────┬───────────────────────┬─────────────────────────┤
│            │                       │                         │
│  Sidebar   │     File List          │     Preview Panel       │
│  侧边栏     │     文件列表           │     预览面板             │
│            │                       │                         │
│  - 全部文件 │  文件名 | 时间 | 大小  │  WebView实时渲染         │
│  - 文件夹   │                       │                         │
│  - 标签     │                       │                         │
│  - 回收站   │                       │                         │
│            │                       │                         │
├────────────┴───────────────────────┴─────────────────────────┤
│  Footer: 版本历史面板（可收起）                                │
└────────────────────────────────────────────────────────────────┘
```

### 4.2 组件说明
- **Sidebar**: 树形文件夹目录、标签筛选、快速访问
- **FileList**: 文件卡片/列表双视图切换、多选操作、排序筛选
- **Preview**: 内置WebView渲染、工具栏（全屏/外部打开/删除）
- **VersionPanel**: 版本时间线、一键回滚、版本对比入口

---

## 5. 开发计划

### 5.1 MVP阶段 (V1.0) ✅ 已完成
- [x] 项目骨架搭建（Electron + Vue3 + Vite）
- [x] 主进程基础能力（窗口管理、IPC通信）
- [x] SQLite数据库初始化
- [x] 仓库目录选择和文件扫描
- [x] 树形文件夹组件
- [x] 文件列表组件
- [x] 内置WebView预览
- [x] 版本快照和回滚
- [x] 搜索和排序功能
- [x] 文件删除功能
- [x] 标签系统
- [x] 设置面板

### 5.2 完善阶段 (V1.1)
- [ ] 元数据自动提取优化
- [ ] 界面优化和交互完善
- [ ] 打包发布配置

### 5.3 插件阶段 (V2.0) 暂缓
- [ ] Chrome插件开发
- [ ] 插件与客户端通信
- [ ] 自动导入功能

---

## 6. 里程碑

| 阶段 | 目标 | 状态 | 完成日期 |
|------|------|------|----------|
| MVP | 基础功能可用 | ✅ 已完成 | 2026-05-12 |
| V1.1 | 功能完善、界面优化、打包发布 | 进行中 | - |
| V2.0 | 浏览器插件 | 暂缓 | - |